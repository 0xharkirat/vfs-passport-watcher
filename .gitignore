node_modules/
dist/
out/
.DS_Store
*.log
# Electron userData (browser profile, screenshots, user config) live outside the repo,
# but ignore any local stray copies too.
browser-profile/
screenshots/
config.json
# Playwright MCP scratch dumps
.playwright-mcp/
# Playwright bundled browsers (downloaded by postinstall, ~200MB)
playwright-browsers/
